# Pipeline dependencies (Python standard library only for detection logic)
# No external dependencies required for run_pipeline.py

# Dashboard dependencies (FastAPI web interface)
fastapi==0.109.0
uvicorn==0.27.0
